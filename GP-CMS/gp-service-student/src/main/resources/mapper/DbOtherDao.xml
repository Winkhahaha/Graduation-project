<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.mineok.dao.DbOtherDao">


	<select id="other_student_list" resultType="org.mineok.vo.StuOtherVo">
		SELECT
		s.stu_name,
		s.stu_id,
		t.topic_id,
		t.topic_name,
		z.sum_score,
		ot.id,
		ot.sum_score2
		FROM
		tb_student s
		LEFT JOIN tb_topic t ON s.topic_id = t.topic_id
		LEFT JOIN tb_db_zdjs z ON s.topic_id = z.topic_id
		LEFT JOIN tb_db_other ot ON s.topic_id = ot.topic_id
		WHERE
		z.conclusion = 1
		<if test="_parameter !=null and _parameter!=''">
			AND t.tid = #{tid}
		</if>
		ORDER BY z.`schedule`
	</select>
</mapper>